<Controls:MetroWindow x:Class="Spelling_Bee.RequestStringWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RequestStringWindow" Style="{DynamicResource BaseWindowStyle}" SizeToContent="Height" Width="220">
    <Grid>
        <Label x:Name="lblTitle" Content="Title" Style="{DynamicResource StaffTitleLabel}"/>
        <TextBox x:Name="txtInput" Margin="10, 30, 10, 40"/>
        <Button x:Name="btnSubmit" Content="Submit" Margin="0, 0, 10, 10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="btnSubmit_Click"/>
    </Grid>
</Controls:MetroWindow>
